# Agent Loop

Reserved for supervised autonomous agent orchestration.

The loop should claim scoped tasks, create isolated worktrees, run coding agents, run checks, request QA review, and produce patches for human/architect approval.
